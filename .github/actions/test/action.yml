name: Test

description: Run checks and tests

runs:
  using: composite
  steps:
    - shell: bash
      run: bun install --frozen-lockfile
    - shell: bash
      run: bun run build
    - shell: bash
      run: bun run lint
    - shell: bash
      run: bun test
