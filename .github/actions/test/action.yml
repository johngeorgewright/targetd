name: Test

description: Run checks and tests

runs:
  using: composite
  steps:
    - uses: ./.github/actions/install-build
    - shell: bash
      run: bun run lint
    - shell: bash
      run: bun test
